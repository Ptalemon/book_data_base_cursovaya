#include "transaction.h"

Transaction::Transaction(student student_data, string issue_date, string return_date,
                         string librarian):Student(student_data){
    transaction_data =
}

